1. **2026-09-23 — Original clients.** WEB handled playlist/channel listings; VISIONOS handled audio. Anonymous throughout. Channel uploads use `UC` → `UU`. VISIONOS responses omitted publication dates.

2. **2026-09-23 — Per-video WEB dates (superseded).** WEB returned exact dates even with `UNPLAYABLE`; VISIONOS still supplied audio. Recovered eight dates for `PL13A9D0E9048D3941`. Worked, but required one extra request per missing date and would scale poorly.

3. **2026-09-24 — Flat sync only (current).** Sync/diffing uses anonymous WEB playlist pages and continuations. Only downloads fetch per-video info, using VISIONOS. Dates come from listing labels, following yt-dlp’s `youtubetab:approximate_date` (reference: `c7fb478`). Use calendar subtraction and nearest-unit rounding; truncation caused a one-day mismatch. Missing dates never trigger per-video lookups. Live results matched yt-dlp. All eight test videos showed “15y ago”, so listing metadata cannot distinguish their exact dates.

4. **2026-09-24 — Parallel audio downloads.** Anonymous VISIONOS still supplies audio-only adaptive formats; select the highest-bitrate AAC/M4A available, otherwise audio-only fallback. Reject video, DRM, OTF, and missing/invalid content lengths. Follow [YouTube.js's range query strategy](https://github.com/LuanRT/YouTube.js/blob/main/src/utils/FormatUtils.ts): four concurrent 1 MiB ranges, three attempts per failed range, 30 seconds per request including its body, five minutes per full download attempt. Accept exact-length HTTP 200 ranges and validate Content-Range on HTTP 206. Live `FAaMG_3Lwug` selected itag 140 (130,689 bit/s, 5,259,873 bytes); previous sequential 10 MiB Range-header download took 1.45 s, parallel query ranges took 0.95 s and 0.59 s, with identical SHA-256 hashes. This sample confirms correctness and faster transfer here; other CDN/network conditions remain unverified. No cookies used.

5. **2026-09-24 — Podcast descriptions and hidden entries.** Anonymous WEB listing of `PLipBN7O7_H3oq9oDRWagdZUoBOV81GCkT` returned its description as a `Message`, plus an INFO alert that unavailable videos were hidden. Treating any message as incomplete incorrectly rejected the playlist. Default pagination returned 500 entries against “504 episodes”. Browsing `VL` + playlist ID with `params: "wgYCCAA="` (yt-dlp's show-unavailable strategy) returned all 504 across five flat pages, matching `yt-dlp --ignore-config --flat-playlist --skip-download --dump-single-json` 2026.08.19 IDs and order. Four unavailable `LockupView` entries retained IDs but had no metadata; retain these as unavailable placeholders. Sync now requests this listing directly, allows descriptive messages and INFO alerts, and still rejects non-INFO alerts and count mismatches. No per-video requests or cookies used for listing checks.

6. **2026-09-24 — Channel handle resolution.** One anonymous HTML GET each for `https://www.youtube.com/@RyanFleury/videos` and `https://www.youtube.com/@RyanFleury/` exposed canonical channel `UCCsdwE2z_kRL3vfVsd5OGyA`, matching complete `yt-dlp --ignore-config --flat-playlist --skip-download --dump-single-json` listings. Resolve channel URLs from the canonical link before creating an Innertube session, replacing `resolveURL`; unrelated recommended-channel IDs are ignored. Uploads still use the existing flat `UU` playlist strategy. HTTP errors or missing canonical channel metadata fail explicitly. Consent-page and regional variants remain unverified. No per-video requests or cookies used in these checks.

7. **2026-09-24 — Consent redirect reproduced in the app client.** The preceding live checks used Bun, while the bridge check mocked HTTP. A real Rust/QuickJS request instead followed a redirect to a Polish YouTube consent page and reproduced “Channel not found”. Initialize the in-memory jar with the reject-optional-cookies preference documented by [NewPipe](<https://teamnewpipe.github.io/NewPipeExtractor/javadoc/org/schabi/newpipe/extractor/services/youtube/YoutubeParsingHelper.html#setConsentAccepted(boolean)>), preserving imported choices. Both handle forms then resolved in the app client; the complete flat uploads scan returned 12 entries, matching yt-dlp. Remaining consent responses now produce a specific error. No account credentials or per-video requests used.

8. **2026-09-24 — Live/upcoming entries in flat listings.** Modern `LockupView` entries carry badges under either `ThumbnailBottomOverlayView` or `ThumbnailOverlayBadgeView`. Anonymous WEB uses the English “Upcoming” label with a default badge style, and a dedicated LIVE style for active streams. Read both overlay forms, retaining non-downloadable placeholders in listing counts; classic entries also expose scheduled event metadata. The app's complete 12-entry uploads scan now marks `AmUPnXrZ9J0` (“N64 3D Rendering From Scratch”) unavailable, matching yt-dlp's flat `is_upcoming` result. Archived streams remain eligible. During audio resolution only, VISIONOS live/upcoming flags or `LIVE_STREAM_OFFLINE` defer the episode until the next scan instead of causing repeated resolution attempts. No per-video requests used for the listing check.

9. **2026-09-24 — Local WEB sessions.** youtubei.js 18.0.0 can generate a local WEB session, but local generation alone still attempts the config endpoint; offline setup also disables `retrieve_innertube_config` and player retrieval. A live check with 18.1.0 used this anonymous local-session strategy to return all 504 entries of `PLipBN7O7_H3oq9oDRWagdZUoBOV81GCkT` through final continuations, including four unavailable modern `LockupView` placeholders and listing age labels. IDs/order, titles, duration fields, and availability matched the existing remotely initialized client; IDs/order also matched complete `yt-dlp --ignore-config --flat-playlist --skip-download --dump-single-json`. No account cookies, per-video info, or media requests used. Authentication, player execution, and broader playlist variants remain unverified with this setup; the application's existing WEB listing and VISIONOS download strategies remain current.
